# Database Configuration
DB_NAME=mareo_envios
DB_USERNAME=mareo_user
DB_PASSWORD=mareo_pass
DB_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# Application Configuration
APP_PORT=8080

# Spring Profiles
SPRING_PROFILES_ACTIVE=docker

# Database URL (auto-generated from above)
# This will be constructed as: jdbc:postgresql://db:${DB_PORT}/${DB_NAME}